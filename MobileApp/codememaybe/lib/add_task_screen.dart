import 'package:flutter/material.dart';

class AddTaskScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dodawanie tasków'),
      ),
      body: Center(
        child: Text('Dodawanie tasków tasków'),
      ),
    );
  }
}
