import 'package:flutter/material.dart';

class MyTaksScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Moja Lista tasków'),
      ),
      body: Center(
        child: Text('Moja Lista tasków'),
      ),
    );
  }
}
